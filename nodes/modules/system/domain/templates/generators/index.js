const area = require("./area");
const generateAverageAreaSensor = require("./old/generateAverageAreaSensor");
const generateDynamicTemplates = require("./generateDynamicTemplates");
const generateTemplates = require("./generateDynamicTemplates");

module.exports = {
    area,
    generateAverageAreaSensor,
    generateDynamicTemplates,
    generateTemplates,
};
